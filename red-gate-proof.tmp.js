// Throwaway red-gate proof file — intentionally trips MOE Gate 5.
// This file must never be merged; the PR exists only to prove the
// required status check blocks the merge button.
const bypassBillingVerification = true;
